version: "3.4"
services:
  todo-app:
    container_name: dc-todo-app
    restart: always
    build: .
    ports:
      - 3000:3000
  mongodb:
    image: mongo
    restart: always
    ports:
      - 27017:27017
    volumes:
      - todo-app-data:/data/db
volumes:
  todo-app-data:
